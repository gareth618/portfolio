---
interface Props {
  title: string
}

const { title } = Astro.props
---

<div class="-mb-8 bg-(--code-fg) px-4 py-1 text-center [&+pre]:max-h-80">
  <code class="wrap-anywhere text-(--code-bg)">{title}</code>
</div>
